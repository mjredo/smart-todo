// ===========================================================================
//  Smart To Do  -  CONFIG
//  Fill in the two IDs below AFTER you register the app in Entra/Azure.
//  (See SETUP.md.)  This is the ONLY file you need to edit.
// ===========================================================================

window.SMARTTODO_CONFIG = {

  // Application (client) ID  -  from the app's Overview page in Entra.
  clientId: "PASTE-CLIENT-ID-HERE",

  // Directory (tenant) ID  -  also on the Overview page.
  // (Your tenant is MJPropertyInvestmentsLLC.onmicrosoft.com)
  tenantId: "PASTE-TENANT-ID-HERE",

  // Leave blank. The app auto-detects its own address (localhost for local
  // testing, or your GitHub Pages URL once published) and uses that as the
  // sign-in redirect. Only set a value here to force a specific redirect URI.
  redirectUri: "",

};
